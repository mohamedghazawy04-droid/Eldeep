rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Legacy Firebase fallback is disabled. Customer and order data use Supabase RLS.
      allow read, write: if false;
    }
  }
}
